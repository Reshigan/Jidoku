# CLAUDE.md — services/agent
The agent's tool set deliberately excludes apjidokal endpoints — builder-only by construction; never add approve tools.
Skills are governed: a skill edit ships only with an eval run showing pass on evals/*.yaml (grow the exam from real
engagement failures). Model-agnostic: MODEL from env; keep the harness independent of any one model. Client values
never enter skills — shapes only (scrubber gate before promotion).
