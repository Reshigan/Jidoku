# SKILL: Integration patterns (T2 draft — pending senior signature)
R-201..R-205: demand idempotent upsert + replay journal before certifying any flow; require single-master
declaration per field in IR; refuse go-live without named error-queue owners; build the cross-system retro-limit
register before first delta; performance-test at month-end shape.
