# CLAUDE.md — jidoka-compiler
Intake: structured workbooks preferred; prose accepted but produces a gap questionnaire, never guessed values.
Every emitted IR value must carry provenance (workbook + cell). LLM assistance may PROPOSE mappings; a human
sign-off converts proposal to signed IR — build that gate into any new intake path. E4 in ROADMAP.
