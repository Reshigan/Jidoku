[project]
name = "jidoka-core"
version = "0.2.0"
description = "JIDOKA domain kernel: IR, planner, registry, ledger, decisions, twin"
requires-python = ">=3.11"
[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[tool.setuptools.packages.find]
where = ["src"]
