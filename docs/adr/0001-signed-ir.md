# ADR-0001: IR executes only signed intent; open DPs block planning
Status: accepted. LLM-era config automation fails through invented values. We make invention structurally
impossible: unsigned sources are unloadable (IRValidationError); decision_point markers hard-block plan().
Consequence: slower intake than "just do it" tools — accepted; it is the product's trust basis.
